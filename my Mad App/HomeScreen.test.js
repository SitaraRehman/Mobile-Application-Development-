// Create a mock for AsyncStorage
// Minimal sanity test to verify Jest runs in this environment.
describe('Sanity', () => {
  it('verifies the test runner is working', () => {
    expect(1 + 1).toBe(2);
  });
});